#include <iostream>
#include "Lista.h"

struct mueble {
	int posicion;//laterla izquierdo a a la pared izquierda EJE X
	int anchura;//ANCHURA EJE Y
};

class cocina {
	
	public:
		
		
	private:
		struct Nodo{
			T elto;
			nodo* sig;
			nodo(T e, nodo * p = nullptr): elto(e), sig(p){}	
		};
		
		Nodo* L;
		Lista<mueble> m_;
};
